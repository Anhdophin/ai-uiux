(function(){
function mount(){ document.querySelectorAll('[data-background="gold-dust"]').forEach(host=>{ if(host.querySelector(':scope > .gold-dust-canvas')) return; const c=document.createElement('canvas'); c.className='gold-dust-canvas'; host.prepend(c); const ctx=c.getContext('2d'); let w=0,h=0,dpr=Math.min(window.devicePixelRatio||1,2), parts=[];
function resize(){ const r=host.getBoundingClientRect(); w=Math.max(1,Math.floor(r.width)); h=Math.max(1,Math.floor(r.height)); c.width=w*dpr; c.height=h*dpr; c.style.width=w+'px'; c.style.height=h+'px'; ctx.setTransform(dpr,0,0,dpr,0,0); parts=Array.from({length:Math.max(90,Math.floor(w/12))},()=>({x:Math.random()*w,y:Math.random()*h,r:.6+Math.random()*2.2,vy:.08+Math.random()*.45,vx:(Math.random()-.5)*.15,a:.2+Math.random()*.7})); }
function draw(){ ctx.clearRect(0,0,w,h); for(const p of parts){ p.y += p.vy; p.x += p.vx; if(p.y>h+8){ p.y=-8; p.x=Math.random()*w; } if(p.x<0) p.x=w; if(p.x>w) p.x=0; ctx.fillStyle='rgba(255,210,90,'+p.a+')'; ctx.shadowBlur=10; ctx.shadowColor='rgba(255,199,80,.7)'; ctx.beginPath(); ctx.arc(p.x,p.y,p.r,0,Math.PI*2); ctx.fill(); } ctx.shadowBlur=0; requestAnimationFrame(draw);} resize(); window.addEventListener('resize',resize,{passive:true}); requestAnimationFrame(draw); }); }
if(document.readyState==='loading') document.addEventListener('DOMContentLoaded',mount,{once:true}); else mount();
})();
