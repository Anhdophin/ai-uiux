(function(){
function mount(){ document.querySelectorAll('[data-background="particles-neon"]').forEach(host=>{ if(host.querySelector(':scope > .particles-neon-canvas')) return; const c=document.createElement('canvas'); c.className='particles-neon-canvas'; host.prepend(c); const ctx=c.getContext('2d'); let w=0,h=0,dpr=Math.min(window.devicePixelRatio||1,2); let pts=[]; const colors=['#ff2e82','#57d6ff','#ff5fb2']; function resize(){ const r=host.getBoundingClientRect(); w=Math.max(1,Math.floor(r.width)); h=Math.max(1,Math.floor(r.height)); c.width=w*dpr; c.height=h*dpr; c.style.width=w+'px'; c.style.height=h+'px'; ctx.setTransform(dpr,0,0,dpr,0,0); pts=Array.from({length:Math.max(30,Math.floor(w/26))},()=>({x:Math.random()*w, y:h*(.55+.45*Math.random()), r:3+Math.random()*8, vy:.1+Math.random()*.35, drift:(Math.random()-.5)*.18, a:.35+Math.random()*.4, color:colors[(Math.random()*colors.length)|0]})); }
function draw(){ ctx.clearRect(0,0,w,h); for(const p of pts){ p.y -= p.vy; p.x += p.drift; if(p.y < -20){ p.y = h+10; p.x = Math.random()*w; } ctx.beginPath(); ctx.fillStyle=p.color; ctx.shadowBlur=18; ctx.shadowColor=p.color; ctx.globalAlpha=p.a; ctx.arc(p.x,p.y,p.r,0,Math.PI*2); ctx.fill(); ctx.globalAlpha=.2; ctx.beginPath(); ctx.arc(p.x,p.y,p.r*2.6,0,Math.PI*2); ctx.fill(); ctx.globalAlpha=1; } requestAnimationFrame(draw);} resize(); window.addEventListener('resize',resize,{passive:true}); requestAnimationFrame(draw); }); }
if(document.readyState==='loading') document.addEventListener('DOMContentLoaded',mount,{once:true}); else mount();
})();
