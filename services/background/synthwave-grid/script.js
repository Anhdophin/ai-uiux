(function(){
function mount(){ document.querySelectorAll('[data-background="synthwave-grid"]').forEach(host=>{ if(host.querySelector(':scope > .synthwave-grid-canvas')) return; const c=document.createElement('canvas'); c.className='synthwave-grid-canvas'; host.prepend(c); const ctx=c.getContext('2d'); let w=0,h=0,dpr=Math.min(window.devicePixelRatio||1,2); let t=0;
function resize(){ const r=host.getBoundingClientRect(); w=Math.max(1,Math.floor(r.width)); h=Math.max(1,Math.floor(r.height)); c.width=w*dpr; c.height=h*dpr; c.style.width=w+'px'; c.style.height=h+'px'; ctx.setTransform(dpr,0,0,dpr,0,0);} 
function draw(){ t+=0.018; ctx.clearRect(0,0,w,h); const horizon=h*0.52; ctx.strokeStyle='rgba(255,90,240,.7)'; ctx.lineWidth=1; for(let i=0;i<20;i++){ const y=horizon + ((h-horizon)/20)*i; const bend=Math.sin(t+i*.35)*8; ctx.beginPath(); ctx.moveTo(0,y+bend); ctx.lineTo(w,y-bend); ctx.stroke(); } for(let x=-w;x<w*2;x+=w/12){ ctx.beginPath(); ctx.moveTo(w/2,horizon); ctx.lineTo(x,h); ctx.stroke(); } ctx.strokeStyle='rgba(255,120,255,.55)'; for(let m=0;m<8;m++){ const base=horizon + m*8; ctx.beginPath(); for(let x=0;x<=w;x+=26){ const y=base + Math.sin((x*.015)+t*2+m)*7; ctx.lineTo(x,y); } ctx.stroke(); } requestAnimationFrame(draw);} resize(); window.addEventListener('resize',resize,{passive:true}); requestAnimationFrame(draw); }); }
if(document.readyState==='loading') document.addEventListener('DOMContentLoaded',mount,{once:true}); else mount();
})();
