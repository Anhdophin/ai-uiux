# Schema Notes

- Pydantic schema dùng cho API contract, không nhất thiết là DB schema cuối cùng.
- Khi tăng phiên bản field persistent, ưu tiên thêm field hơn là sửa nghĩa field cũ.
- Seed JSON nên giữ id ổn định.
